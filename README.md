Sources for the plugin `plugin.googleAnalytics`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.googleAnalytics" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
